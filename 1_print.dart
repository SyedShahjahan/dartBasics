import "dart:core"; // Package will be included by default. So no need to include externally

main() { //Can also write main() with void as datatype like void main() {}
    print("Hello World!!");
}